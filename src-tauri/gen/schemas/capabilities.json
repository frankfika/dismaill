{"default":{"identifier":"default","description":"Default capability for the main window — Phase 0 minimum surface area. The renderer does not import any Tauri plugin module (fs/dialog/store/os), so those permissions are dropped to keep the attack surface small.","local":true,"windows":["main"],"permissions":["core:default","shell:allow-open"]}}